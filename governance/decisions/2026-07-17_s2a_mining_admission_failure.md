# S2a mining admission decision

> **Superseded for successor design:** this control-only successor authorization
> was written after the live proposal had already activated Section 25. It is
> therefore void wherever it conflicts with the sole proposal authority. S2a
> v2 must be KAN-centered, use new scientific seeds and topology, and must not
> reuse v1 membership. The sealed v1 failure classification remains historical
> custody evidence. See the KAN-first directive and the in-flight supersession
> incident.

## Decision

Seal `s2_plan_c_vertical_v1` as a retryable protocol failure. Do not rerun its 256-attempt entitlement, do not publish any factor library from its terminal directories, and do not create the v1 development opening.

## Classification

- Status: `FAILED (NoExecutableWithinBudget)`
- Failure type: implementation/protocol
- Scientific method judgment: not evaluated against baseline
- Development test: unopened
- Graph control: locked

## Authorized successor scope

A successor protocol may change only negative-control construction so that the permutation library is size matched without weakening observed-arm admission. It must reuse the exact deterministic candidate topology, preserve fresh permutation scoring and all provenance, use new no-replace artifact names, and pass a new implementation audit before opening development results.

Generating or searching additional candidates is not authorized by this decision.
