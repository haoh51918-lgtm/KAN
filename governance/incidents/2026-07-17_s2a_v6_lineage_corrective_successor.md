# Governance authorization: S2a v6 lineage-corrective successor

- **Date**: 2026-07-17 UTC
- **Predecessor**: `s2a_kan_e3_vertical_v5`
- **Successor**: `s2a_kan_e3_vertical_v6`
- **Authorization class**: corrective adaptive successor after development infrastructure failure
- **Predecessor observation**: inconclusive infrastructure failure with quarantined development outputs
- **Prospective one-shot status**: spent; v6 is repeated developmental evidence
- **Formal promotion**: forbidden

## Failure disposition

v5 completed and immutably published its seven-part mining topology. It then
consumed its single development opening. Alpha158, selected KAN, and typed
GP/SR each completed the exact 966-date Quanta execution and remained private
inside the development transaction. Before the matched black-box arm entered
Quanta, the loader rejected its immutable mining artifact with
`matched blackbox pairing differs from selected KAN lineage`.

The transaction deleted every staged arm bundle and terminalized all five
evaluation targets plus the decision target. Each terminal target contains
only a read-only `terminal_failure.json` with an empty partial-file inventory.
No development manifest, metric bundle, factor-library comparison, or decision
was published. Three raw MLflow run trees remain for custody only. They are
quarantined, non-authoritative, and forbidden as v6 scientific inputs or
performance evidence.

The failure is an order-sensitive identity-check defect, not a scientific
lineage mismatch. The top mining manifest stores KAN lineage as a canonical
factor-ID-keyed mapping. The matched MLP manifest preserves selection order so
that prediction columns, control receipts, tensor trajectories, seeds, and
bootstrap weights remain positionally bound. The two artifacts contain the
same seven factor IDs and the same seven exact ID-to-global-attempt mappings,
but their display order differs. The loader incorrectly compared two ordered
lists against dictionary iteration order.

## Permitted v6 changes

1. Move the protocol identity and every writable mining, evaluation, decision,
   authority, opening, recovery, report, and tracking path to v6.
2. Replace the loader's ordered-list equality with exact equality of the
   factor-ID-to-global-attempt mapping. Preserve the matched-control order and
   all positional bindings to predictions, receipts, trajectories, seeds, and
   bootstrap weights.
3. Strengthen top-bundle staging to reject crossed factor-ID/global-attempt
   pairings even when the separate ID and index sets are unchanged.
4. Retain the v5 exact-calendar correction unchanged.
5. Admit only the precise predecessor-custody class
   `inconclusive_infrastructure_with_quarantined_development_outputs`.
6. Label v6 outputs `corrective_adaptive_repeated_development_screen`; never
   represent them as a fresh prospective one-shot or final confirmation.
7. Reuse the byte-identical isolated dependency environment and wheelhouse,
   but use new v6 runtime evidence, locks, tracking, and topology paths.

No scientific field may change. Search space, attempt budgets, seeds,
optimization, admission thresholds, dates, purge, warm-up, Quanta pins,
LightGBM settings, portfolio construction, costs, metrics, bootstrap,
guardrails, and decision thresholds remain equivalent after removing protocol
identity, writable paths, and evidence-class metadata.

## Quarantine and custody

The complete v5 chain is predecessor evidence only: configuration,
preregistration, locks, runtime evidence, seven mining topology targets and
payloads, authority ledger, consumed openings, four development arm
consumptions, six terminal-failure records, three MLflow run trees, and all
reports. v5 mining members are not copied into the active v6 topology. v6 must
rerun the deterministic mining chain under its own identities.

## Required gates before v6 label access

1. A regression test accepts a valid matched-control pairing whose selection
   order differs from canonical lineage order.
2. A negative regression test rejects crossed global-attempt assignments with
   unchanged ID and index sets.
3. The complete matched-control loading seam validates every receipt,
   trajectory, tensor, seed, bootstrap, and replay binding without reordering.
4. Full tests, Ruff, offline dependency verification, Quanta pins, provider
   identity, GPU identity, deterministic state, and both lock audits pass.
5. The v6 base lock binds the complete v5 quarantine and v6 runtime closure.
6. A new v6 implementation lock is written exclusively and verified live
   before mining entitlement consumption.

Even if v6 passes every autonomous gate, it remains corrective repeated
development evidence. Formal promotion still requires human blind review and
genuinely unseen confirmation evidence.
