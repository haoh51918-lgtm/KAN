# S2a MIRAGE-KAN exact-mining-rebind corrective preregistration v8

v8 inherits the complete v7 preregistration and changes only the canonical-byte
algorithm used to verify the original v6 authority receipt. The algorithm must
match `governance/authority.py`: sorted, two-space-indented JSON plus exactly one
trailing newline. The real v6 sequence-1 receipt is a mandatory regression.

v7 is preserved as a terminal pre-opening software-validation failure. It did
not create a rebind receipt, mining entitlement, development opening, evaluation,
or scientific result. v8 continues to reference the byte-exact full v6 mining
topology. No mining, reselection, reordering, threshold change, retuning, source
copy, or source mutation is allowed.

All v7 scientific, evaluation, Quanta, graph-authorization, evidence-class,
confirmation-set, and human-review constraints remain unchanged. The v6
development opening remains unconsumed, and 2026 H1 remains sealed.
